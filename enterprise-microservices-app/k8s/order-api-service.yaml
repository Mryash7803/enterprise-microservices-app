apiVersion: v1
kind: Service
metadata:
  name: order-api
  namespace: microservices
spec:
  type: NodePort
  selector:
    app: order-api
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
